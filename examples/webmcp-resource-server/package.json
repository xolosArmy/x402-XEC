{
  "name": "webmcp-resource-server",
  "version": "0.1.0",
  "private": true,
  "description": "Static Payment-Required resource server for WebMCP Challenge Gate H2A",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/index.ts",
    "test": "tsx --test test/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  }
}
